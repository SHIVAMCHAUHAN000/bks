# Local API configuration. This file is ignored by Git: never commit real keys.
PORT=8080
DATABASE_PATH=data/leaddesk.db
PUBLIC_API_URL=http://localhost:8080

# Add your Resend credentials to enable real sending. Leave blank for simulated local sends.
RESEND_API_KEY=
MAIL_FROM=
